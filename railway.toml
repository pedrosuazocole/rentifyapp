[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build && npx prisma migrate deploy && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
